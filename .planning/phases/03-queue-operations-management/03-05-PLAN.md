---
phase: 03-queue-operations-management
plan: 05
type: execute
wave: 3
depends_on: ["03-03", "03-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Volunteer can view full queue showing all users, positions, and statuses"
    - "Volunteer can drag users to different positions to manually reorder queue"
    - "When user removed from queue, remaining users auto-reposition without gaps"
    - "Service user can view their current position in queue in real-time"
    - "Volunteer can search queue by user name and find results instantly"
    - "Queue positions update across all devices after reorder"
    - "Volunteer can manually add users without phones to the queue"
---

<objective>
Human verification of complete queue management functionality.

Purpose: Validate all Phase 3 requirements work correctly in the running application before marking phase complete.
Output: Verified queue operations across volunteer and service user flows.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-queue-operations-management/03-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete queue management system including:
- Volunteer queue view with position numbers on cards
- Drag-and-drop reordering with confirmation dialog
- Auto-repositioning when users removed from queue
- Service user queue position display with real-time updates
- Search filtering by name
- Manual add user flow with optional phone (already implemented in Phase 2)
  </what-built>
  <how-to-verify>
**Setup:**
1. Start dev server: `npx expo start` and `npx convex dev` in separate terminals
2. Open app on two devices/simulators (or web + mobile)

**Test 1: Volunteer Queue View (QUEUE-02)**
1. Log in as admin, create a session
2. Log in as volunteer, scan session QR to join
3. Add 3-4 test users via "Add User" FAB
4. Verify: Queue tab shows all users with position numbers (#1, #2, #3, #4)

**Test 2: Search Filtering (QUEUE-06)**
1. In Queue tab, type a name in the search bar
2. Verify: List filters instantly to show only matching names
3. Clear search, verify: Full list returns
4. Note: Search is by NAME only (not phone) per CONTEXT.md decision

**Test 3: Drag-and-Drop Reordering (QUEUE-05)**
1. Long-press on user #3's card
2. Drag to position #1
3. Verify: Confirmation dialog appears "Move [Name] to position 1?"
4. Tap "Move"
5. Verify: User is now #1, others shifted accordingly

**Test 4: Auto-Reposition on Removal (QUEUE-04)**
1. Note current positions (1, 2, 3, 4)
2. Remove user #2 (tap Remove, confirm)
3. Verify: Former #3 is now #2, former #4 is now #3
4. No gaps in position sequence

**Test 5: Service User Queue Position (QUEUE-01)**
1. On second device, log in as service user
2. Register and join queue
3. Verify: Status screen shows current position number
4. On volunteer device, remove a user ahead of this user
5. Verify: Position number updates automatically on service user device

**Test 6: Real-time Cross-Device Sync (QUEUE-03)**
1. Open queue view on two volunteer devices
2. On device A, add a new user
3. Verify: User appears on device B without refresh
4. On device A, reorder queue
5. Verify: New order reflected on device B within 2 seconds
6. Positions match exactly across both devices

**Test 7: Manual Add User with Optional Phone (QUEUE-08)**
1. On volunteer device, tap "Add User" FAB
2. Fill in: First name, Last name (required), skip phone number
3. Fill in: Living condition, estimated loads
4. Tap Submit
5. Verify: User added to queue successfully without phone number
6. Verify: No validation error for missing phone
7. Repeat with phone number filled in
8. Verify: Both users appear in queue correctly

**Expected Results:**
- All position numbers display correctly and update in real-time
- Drag-and-drop feels smooth (60fps) with confirmation dialog
- Removing users closes position gaps immediately
- Service user sees their position update when queue changes
- Search filters instantly as you type (by name only)
- Queue syncs across devices within 2 seconds
- Users without phones can be added to queue successfully
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human verification of all Phase 3 success criteria from ROADMAP.md:
1. Service user can view their current position in queue in real-time
2. Volunteer can view full queue showing all users, positions, and statuses
3. When user removed from queue, remaining users auto-reposition without gaps
4. Volunteer can drag users to different positions to manually reorder queue
5. Volunteer can search queue by user name and find results instantly
6. Volunteer can manually add users without phones to the queue via intake form
</verification>

<success_criteria>
All 7 test scenarios pass without errors or unexpected behavior.
</success_criteria>

<output>
After approval, create `.planning/phases/03-queue-operations-management/03-05-SUMMARY.md`
</output>
