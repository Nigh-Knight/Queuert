---
phase: 01-real-time-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/_layout.tsx
  - .env.local
  - package.json
autonomous: true

must_haves:
  truths:
    - "ConvexProvider wraps app root"
    - "useQuery and useMutation hooks are importable in components"
    - "App starts without errors after Convex integration"
  artifacts:
    - path: "app/_layout.tsx"
      provides: "ConvexProvider wrapper at app root"
      contains: "ConvexProvider"
    - path: ".env.local"
      provides: "Convex URL configuration"
      contains: "EXPO_PUBLIC_CONVEX_URL"
  key_links:
    - from: "app/_layout.tsx"
      to: "convex/_generated/api"
      via: "ConvexReactClient initialization"
      pattern: "ConvexReactClient"
---

<objective>
Set up ConvexProvider at app root and install required dependencies for Phase 1.

Purpose: Establish the real-time infrastructure foundation that all other Phase 1 work depends on. Without ConvexProvider, useQuery/useMutation hooks will not work.

Output: App boots with Convex client initialized, new dependencies installed (datetimepicker, qrcode-svg, react-native-svg), environment configured.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-real-time-infrastructure-&-session-management/01-RESEARCH.md

@app/_layout.tsx
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Phase 1 Dependencies</name>
  <files>package.json</files>
  <action>
Install the following dependencies:

1. Date/time picker for session scheduling:
   `npx expo install @react-native-community/datetimepicker`

2. QR code generation for volunteer codes:
   `npm install react-native-qrcode-svg react-native-svg`

Note: expo-crypto is bundled with Expo SDK 54, no install needed.
Note: convex ^1.31.6 is already installed.

After installation, verify package.json contains all new dependencies.
  </action>
  <verify>
Run `npm list @react-native-community/datetimepicker react-native-qrcode-svg react-native-svg` - all should show installed versions without errors.
  </verify>
  <done>
package.json includes @react-native-community/datetimepicker, react-native-qrcode-svg, and react-native-svg as dependencies.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Convex Environment</name>
  <files>.env.local</files>
  <action>
1. Run `npx convex dev` to ensure Convex project is linked and get the deployment URL.
   - If prompted to create a new project, create one named "queuert"
   - This generates/updates .env.local with CONVEX_URL

2. Add EXPO_PUBLIC_ prefixed variable for React Native access:
   - Copy CONVEX_URL value
   - Add line: EXPO_PUBLIC_CONVEX_URL=<copied_value>

3. Ensure .env.local is in .gitignore (contains secrets)

The EXPO_PUBLIC_ prefix is required for Expo to expose the variable to the client bundle.
  </action>
  <verify>
Check that `.env.local` contains:
- CONVEX_URL (from npx convex dev)
- EXPO_PUBLIC_CONVEX_URL (copy of CONVEX_URL)

Run `grep EXPO_PUBLIC_CONVEX_URL .env.local` - should return the URL line.
  </verify>
  <done>
.env.local exists with both CONVEX_URL and EXPO_PUBLIC_CONVEX_URL containing valid Convex deployment URLs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wrap App Root with ConvexProvider</name>
  <files>app/_layout.tsx</files>
  <action>
Update app/_layout.tsx to wrap the existing ThemeProvider with ConvexProvider:

1. Import ConvexProvider and ConvexReactClient from "convex/react"

2. Create Convex client OUTSIDE the component (at module level):
   ```typescript
   const convex = new ConvexReactClient(
     process.env.EXPO_PUBLIC_CONVEX_URL as string,
     { unsavedChangesWarning: false } // Required for React Native
   );
   ```

3. Wrap the existing ThemeProvider with ConvexProvider:
   ```typescript
   <ConvexProvider client={convex}>
     <ThemeProvider value={...}>
       {/* existing Stack and StatusBar */}
     </ThemeProvider>
   </ConvexProvider>
   ```

Important: Keep the existing ThemeProvider, Stack navigation, and StatusBar unchanged. Only add the ConvexProvider wrapper around them.
  </action>
  <verify>
1. Run `npx expo start --web` and verify app loads without errors
2. Check browser console for no Convex-related errors
3. Verify the app still renders the existing screens (tabs, provider)
  </verify>
  <done>
App boots successfully with ConvexProvider wrapping the root. No console errors. Existing navigation still works.
  </done>
</task>

</tasks>

<verification>
1. Run `npm list convex @react-native-community/datetimepicker react-native-qrcode-svg` - all installed
2. Run `grep EXPO_PUBLIC_CONVEX_URL .env.local` - returns URL
3. Run `npx expo start --web` - app boots without errors
4. Check that app/_layout.tsx contains ConvexProvider import and usage
</verification>

<success_criteria>
- All Phase 1 dependencies installed
- Convex environment properly configured with EXPO_PUBLIC_ prefix
- ConvexProvider wraps app root
- App starts successfully on web (and mobile if tested)
- No console errors related to Convex initialization
</success_criteria>

<output>
After completion, create `.planning/phases/01-real-time-infrastructure-&-session-management/01-01-SUMMARY.md`
</output>
