---
phase: 02-authentication-and-role-access
plan: 05
type: execute
wave: 3
depends_on: ["02-01"]
files_modified:
  - convex/auth.ts
  - app/(admin)/verify.tsx
  - app/(admin)/index.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can enter verification code to access admin functions"
    - "Invalid verification code shows error"
    - "Successful verification saves admin session to AsyncStorage"
    - "Admin session persists across app restarts"
  artifacts:
    - path: "app/(admin)/verify.tsx"
      provides: "Admin verification screen"
      min_lines: 60
  key_links:
    - from: "app/(admin)/verify.tsx"
      to: "convex/auth.ts"
      via: "useMutation(api.auth.verifyAdminCode)"
      pattern: "useMutation.*verifyAdminCode"
    - from: "app/(admin)/verify.tsx"
      to: "utils/session-storage.ts"
      via: "SessionStorage.save() on successful verification"
      pattern: "SessionStorage\\.save"
---

<objective>
Wire the admin verification flow to Convex and persist admin sessions.

Purpose: Admins access the app using a verification code. This phase adds the Convex mutation and wires the existing verification UI to save sessions.

Output: Working admin verification with session persistence.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-authentication-and-role-access/02-CONTEXT.md
@.planning/phases/02-authentication-and-role-access/02-RESEARCH.md

# From Plan 01
@utils/session-storage.ts
@convex/auth.ts

# Existing admin screens
@app/(admin)/_layout.tsx
@app/(admin)/index.tsx
@app/(admin)/create-session.tsx

# Existing verification UI (reference)
@components/provider/screens/VerificationScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add admin verification mutation to Convex</name>
  <files>convex/auth.ts</files>
  <action>
Add the admin verification mutation to the existing auth.ts file.

**verifyAdminCode mutation:**
- Args: `{ code: v.string() }`
- Hardcoded valid code: "kepler cool" (from CONTEXT.md - already used in existing UI)
- If code matches (case-insensitive trim), return success
- If code doesn't match, throw "Invalid verification code"

**Return on success:**
```typescript
{
  role: "service_provider" as const,
  verified: true,
  // Note: No sessionId yet - admin creates session after verification
}
```

**Why hardcoded:**
- Per CONTEXT.md, admin verification uses special code "kepler cool"
- No complex auth system needed for this MVP
- Can be migrated to env variable or database later

**Important:** Add this mutation to the EXISTING auth.ts file, don't replace it.
  </action>
  <verify>
- convex/auth.ts includes verifyAdminCode: `grep "verifyAdminCode" convex/auth.ts`
- Convex syncs successfully: `npx convex dev` (no errors)
  </verify>
  <done>
Admin verification mutation exists in Convex auth module
  </done>
</task>

<task type="auto">
  <name>Task 2: Update admin index to check session</name>
  <files>app/(admin)/index.tsx</files>
  <action>
Update the admin index page to check for existing session and route accordingly.

**Current behavior:** Shows admin dashboard directly
**New behavior:**
1. On mount, check AsyncStorage for existing session
2. If session exists with role "service_provider", show dashboard (existing behavior)
3. If no session, redirect to verify screen

**Implementation:**
- Add useEffect with SessionStorage.load()
- Add loading state while checking
- Use router.replace('/(admin)/verify') if no session
- Keep existing dashboard content for when session exists

**Loading state:**
- Show ActivityIndicator centered while checking session
- Prevents flash of wrong content
  </action>
  <verify>
- app/(admin)/index.tsx checks session: `grep "SessionStorage" app/\(admin\)/index.tsx`
- Has redirect to verify screen
  </verify>
  <done>
Admin index checks for session and redirects unauthenticated users to verify
  </done>
</task>

<task type="auto">
  <name>Task 3: Create admin verification screen</name>
  <files>app/(admin)/verify.tsx</files>
  <action>
Create the admin verification screen with Convex integration.

**UI design:**
- Similar to existing VerificationScreen component but simpler
- Title: "Admin Access"
- Description: "Enter the admin verification code"
- 6-digit code input (reuse pattern from VerificationScreen.tsx)
- "Verify" button
- No phone number display (admin auth isn't phone-based)

**Flow:**
1. Admin enters code
2. Call `verifyAdminCode` mutation
3. On success:
   - Save session to AsyncStorage with role "service_provider"
   - Note: sessionId can be undefined for admin (they create sessions)
   - Navigate to admin index (dashboard)
4. On error:
   - Show Alert with error message
   - Clear code input for retry

**State:**
- code: string
- isVerifying: boolean
- error: string | null

**Session data to save:**
```typescript
{
  role: 'service_provider',
  location: '', // Admin can access all locations
  timestamp: Date.now(),
}
```

**Styling:**
- Use existing theme constants
- Match style of other auth screens
- Header with back navigation to role selection
  </action>
  <verify>
- File exists at app/(admin)/verify.tsx
- Calls verifyAdminCode: `grep "verifyAdminCode" app/\(admin\)/verify.tsx`
- Saves session: `grep "SessionStorage" app/\(admin\)/verify.tsx`
- Has code input
  </verify>
  <done>
Admin verification screen validates code and persists session
  </done>
</task>

</tasks>

<verification>
1. verifyAdminCode mutation exists in convex/auth.ts
2. Admin index redirects to verify when no session
3. Verify screen validates code with Convex
4. Successful verification saves session
5. After verification, admin can access dashboard
6. Logout clears session and requires re-verification
</verification>

<success_criteria>
- Admin can enter "kepler cool" and access admin functions
- Invalid code shows error message
- Session persists - admin doesn't re-verify on app restart
- Logging out requires re-verification
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-and-role-access/02-05-SUMMARY.md`
</output>
